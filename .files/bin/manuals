#!/usr/bin/env bash
set -u
MANUALS_DIR="${DOTFILES}/manuals"

if [ $# -eq 0 ]; then
   for a_file in $MANUALS_DIR/*; do
      echo - $(basename $a_file)
   done
elif [ $# -eq 1 ]; then
   printf "\n"
   cat $HOME/.manuals/$1
   printf "\n\n"
else
   echo "innvalid number of arguments"
fi